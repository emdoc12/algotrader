__pycache__/
*.pyc
*.pyo
.env
bot_data.db
cache/
*.pkl
# Runtime data dir only (root-level). NOT daytrader/data/, which is SOURCE —
# the bare "data/" rule silently un-tracked the feeds package for months.
/data/
settings.json
*.db
*.db-shm
*.db-wal
report.html
